<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SHOW ALL USER</title>
    <style>
        table , tr , th {
              border: 2px solid black;
        }
    </style>
</head>

<body>
    <h1>users!</h1>
    <table>
        <tr>
            <th>id</th>
            <th>username</th>
            <th>email</th>
        </tr>
        <% for(user of result){%>
                <tr>
                    <td><%=user.id%></td>
               
                    <td><%=user.username  %></td>
                    <td><%= user.email %></td>
                  <td> <form method="get" action="/user/<%= user.id %>/edit">
                    <button type="submit">edit</button>
                   </form>
                   </td>
                   <td>
                    <form action="/user/<%= user.id %>/del" method="get">
                        <button type="submit">delete</button>
                    </form>
                   </td>
                </tr>
       <% } %>
       <h2><a href="http://localhost:3000/user/add">add</a></h2>

    </table>
</body>
</html>